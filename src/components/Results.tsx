import { TrendingUp, Target, Sparkles, RotateCcw, MessageCircle } from 'lucide-react';
import { AnalysisResult } from '../types/career';

interface ResultsProps {
  results: AnalysisResult;
  onReset: () => void;
  onAskQuestions: (careerTitle: string) => void;
}

export function Results({ results, onReset, onAskQuestions }: ResultsProps) {
  return (
    <div className="min-h-screen px-4 py-12">
      <div className="max-w-6xl mx-auto space-y-8 fade-in">
        <div className="text-center space-y-4">
          <div className="inline-flex items-center gap-2 glass-panel px-4 py-2 mb-2">
            <Sparkles className="w-4 h-4 text-cyan-400 pulse-glow" />
            <span className="text-sm text-cyan-300 font-medium">Analysis Complete</span>
          </div>

          <h2 className="text-4xl md:text-5xl font-bold text-gradient">
            Your Career Recommendations
          </h2>

          <p className="text-lg text-slate-300 max-w-3xl mx-auto">
            {results.summary}
          </p>
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-2 gap-6 mt-12">
          {results.recommendations.map((career, index) => (
            <div
              key={index}
              className="glass-panel-hover p-8 space-y-4 card-appear"
              style={{ animationDelay: `${index * 0.1}s` }}
            >
              <div className="flex items-start justify-between gap-4">
                <div>
                  <h3 className="text-2xl font-bold text-cyan-300 mb-2">
                    {career.title}
                  </h3>
                  <p className="text-slate-400">{career.description}</p>
                </div>
                <div className="glass-panel px-3 py-1.5 text-cyan-300 font-semibold text-sm whitespace-nowrap">
                  #{index + 1}
                </div>
              </div>

              <div className="space-y-3 pt-4 border-t border-white/10">
                <div className="flex gap-3">
                  <Target className="w-5 h-5 text-cyan-400 flex-shrink-0 mt-0.5" />
                  <div>
                    <p className="text-sm font-semibold text-slate-300 mb-1">Why This Fits</p>
                    <p className="text-sm text-slate-400">{career.matchReason}</p>
                  </div>
                </div>

                <div className="flex gap-3">
                  <TrendingUp className="w-5 h-5 text-cyan-400 flex-shrink-0 mt-0.5" />
                  <div>
                    <p className="text-sm font-semibold text-slate-300 mb-1">Growth Outlook</p>
                    <p className="text-sm text-slate-400">{career.growthPotential}</p>
                  </div>
                </div>
              </div>

              <div className="pt-4 border-t border-white/10">
                <p className="text-sm font-semibold text-slate-300 mb-3">Key Skills Required</p>
                <div className="flex flex-wrap gap-2">
                  {career.requiredSkills.map((skill, idx) => (
                    <span
                      key={idx}
                      className="glass-panel px-3 py-1.5 text-xs text-cyan-300 border-cyan-400/20"
                    >
                      {skill}
                    </span>
                  ))}
                </div>
              </div>

              <div className="pt-4">
                <button
                  onClick={() => onAskQuestions(career.title)}
                  className="cyber-button w-full inline-flex items-center justify-center gap-2"
                >
                  <MessageCircle className="w-4 h-4" />
                  <span>Ask Questions About This Career</span>
                </button>
              </div>
            </div>
          ))}
        </div>

        <div className="text-center pt-8">
          <button
            onClick={onReset}
            className="cyber-button-primary inline-flex items-center gap-2"
          >
            <RotateCcw className="w-5 h-5" />
            <span>Start New Analysis</span>
          </button>
        </div>

        <div className="glass-panel p-6 mt-8 text-center">
          <p className="text-sm text-slate-400">
            These recommendations are generated by AI based on your inputs. Consider them as
            starting points for further research and career exploration.
          </p>
        </div>
      </div>
    </div>
  );
}
